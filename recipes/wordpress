#!/bin/bash

function defaults_wordpress {
	DATABASE_PASSWORD="mydbs3cr3t"
}

function install_wordpress {
	source "recipes/general"
	install_general
	install_mysql $DATABASE_PASSWORD && tune_mysql 40
	install_memcached
	install_nginx
	install_php5_fpm
	install_varnish
}
